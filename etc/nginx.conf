server {
    listen 80 default;
    location ^~ /hello/ {
        proxy_pass http://127.0.0.1:8080;
    }
    location ^~ /uploads/ {
        alias /home/box/web/uploads;
    }
    location ~* ^.+\.\w+$ {
        alias /home/box/web/public;
    }
    location / {
        return 404;
    }
}

