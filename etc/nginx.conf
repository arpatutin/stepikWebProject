server {
    listen 80 default;
    location ~* ^+/uploads/ {
        root /home/box/web/uploads;
    }
    location ~* ^+. {
        root /home/arseniy/stepikWebProject/public;
    }
    location / {
        return 404;
    }
}
